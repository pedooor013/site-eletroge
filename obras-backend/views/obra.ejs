<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title><%= obra.nomeObra %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
<h1><%= obra.nomeObra %></h1>
<p><%= obra.descricao %></p>
<img src="<%= obra.imagem %>" alt="Imagem da Obra" width="400">

<div class="progress-container" style="width: 400px; background: #eee; height: 30px; border-radius: 8px; margin-top: 20px;">
    <div style="width: <%= obra.progresso %>%; height: 100%; background: orange; border-radius: 8px;"></div>
</div>

<h3>Categorias:</h3>
<div>
    <% categorias.forEach(c => { %>
        <div style="margin-bottom: 10px;">
            <i class="<%= c.icone %>"></i> <%= c.nome %>
        </div>
    <% }) %>
</div>
</body>
</html>
